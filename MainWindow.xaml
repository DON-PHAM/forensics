<Window x:Class="ImageForensics.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageForensics"
        xmlns:vm="clr-namespace:ImageForensics.ViewModels"
        xmlns:controls="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="Image Forensics" Height="800" Width="1200">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Load Image" Command="{Binding LoadImageCommand}" Margin="0,0,10,0"/>
            <Button Content="EXIF Analysis" Command="{Binding AnalyzeExifCommand}" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding LoadingText}" VerticalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Images -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Original Image -->
                <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Image Source="{Binding OriginalImageSource}" Stretch="Uniform"/>
                </Border>

                <!-- Analyzed Image -->
                <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
                    <Image Source="{Binding AnalyzedImageSource}" Stretch="Uniform"/>
                </Border>
            </Grid>

            <!-- Right Panel - EXIF Info -->
            <Grid Grid.Column="1">
                <ItemsControl ItemsSource="{Binding ExifGroups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Group Header -->
                                <Border Grid.Row="0" Background="LightGray" Padding="5">
                                    <TextBlock Text="{Binding GroupName}" FontWeight="Bold"/>
                                </Border>

                                <!-- Properties -->
                                <ItemsControl Grid.Row="1" ItemsSource="{Binding Properties}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="120"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding Name}" Margin="5"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Value}" Margin="5"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>
    </Grid>
</Window> 